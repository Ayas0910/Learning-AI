[project]
name = "telusko"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Ayas V", email = "ayasmohamed0910@gmail.com" }
]
requires-python = ">=3.10"
dependencies = [
    "python-dotenv>=1.2.3",
    "requests>=2.34.2",
]

[project.scripts]
telusko = "telusko:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
